{
  "name": "@opengeometry/openglyph",
  "version": "0.0.1",
  "description": "Glyph Manager for Threejs Applications",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "clean": "rimraf dist && rimraf example-dist",
    "build:example": "npm run clean && vite build",
    "build:lib": "npm run clean && rollup -c"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.173.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.2",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^12.1.2",
    "@types/three": "^0.173.0",
    "rimraf": "^6.0.1",
    "rollup": "^4.34.8",
    "tslib": "^2.8.1",
    "vite": "^6.1.0"
  }
}
